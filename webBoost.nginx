server {
    listen 80;

    location /images {
        alias /var/www/webBoost/images;
    }

    location /static {
        alias /var/www/webBoost/static;
    }

    location / {
        proxy_pass http://localhost:3000;
    }
}
